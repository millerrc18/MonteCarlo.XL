<UserControl x:Class="MonteCarlo.UI.Controls.SkeletonLoader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border CornerRadius="4" ClipToBounds="True"
            Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Border.Background>
            <SolidColorBrush x:Name="SkeletonBrush" Color="#E2E8F0"/>
        </Border.Background>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <ColorAnimation Storyboard.TargetName="SkeletonBrush"
                                        Storyboard.TargetProperty="Color"
                                        From="#E2E8F0" To="#CBD5E1"
                                        Duration="0:0:0.8"
                                        AutoReverse="True"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
    </Border>
</UserControl>
