using System.Windows.Controls;

namespace MonteCarlo.UI.Views;

/// <summary>
/// Target analysis control — user enters a target value and sees probability of exceeding it.
/// </summary>
public partial class TargetLineControl : UserControl
{
    public TargetLineControl()
    {
        InitializeComponent();
    }
}
