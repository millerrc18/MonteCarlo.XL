<UserControl x:Class="MonteCarlo.UI.Views.InputCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:MonteCarlo.UI.Views">
    <Border Style="{StaticResource Card}" Margin="0,0,0,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Label + Cell Ref + Buttons -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Label}"
                               Style="{StaticResource HeadlineSmall}"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding FullReference}"
                               Style="{StaticResource CaptionText}"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="✕"
                        Style="{StaticResource GhostButton}"
                        Command="{Binding DataContext.RemoveInputCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        CommandParameter="{Binding}"
                        VerticalAlignment="Top"
                        Padding="4,2"
                        FontSize="14"/>
            </Grid>

            <!-- Row 1: Distribution summary -->
            <TextBlock Grid.Row="1"
                       Text="{Binding ParameterSummary}"
                       Style="{StaticResource BodyText}"
                       Margin="0,4,0,0"/>

            <!-- Row 2: Mini preview -->
            <views:DistributionPreviewControl Grid.Row="2"
                                              Points="{Binding PreviewPoints}"
                                              Margin="0,6,0,0"
                                              Height="36"
                                              HorizontalAlignment="Stretch"/>
        </Grid>
    </Border>
</UserControl>
